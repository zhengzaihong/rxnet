# 迁移指南：RxNet Plus 0.5.x → 0.6.0

欢迎使用 RxNet Plus 0.6.0！本版本引入了革命性的可插拔适配器架构，将 RxNet 与特定的 HTTP 客户端库解耦，让您可以灵活选择最适合您需求的实现方式。

## 📋 目录

- [新特性](#新特性)
- [破坏性变更](#破坏性变更)
- [迁移步骤](#迁移步骤)
- [新功能详解](#新功能详解)
- [适配器选择指南](#适配器选择指南)
- [拦截器变更](#拦截器变更)
- [CancelToken 改进](#canceltoken-改进)
- [常见迁移场景](#常见迁移场景)
- [常见问题](#常见问题)

---

## 🎉 新特性

### 1. 可插拔适配器架构

RxNet Plus 0.6.0 引入了革命性的适配器系统，完全解耦框架与特定 HTTP 客户端库：

#### 可用适配器

- **DioAdapter**（默认）：基于 Dio 的全功能适配器
  - 完整的 HTTP 功能
  - 高级拦截器支持
  - 真正的请求取消
  - 带进度的文件上传/下载
  - 最适合生产应用

- **HttpAdapter**：基于标准 `http` 包的轻量级适配器
  - 最小依赖
  - 更小的包体积
  - 基础 HTTP 功能
  - 最适合简单应用或关注包大小的场景

- **MockAdapter**：用于单元测试和集成测试的测试适配器
  - 无网络调用
  - 可配置的模拟响应
  - 模拟错误和延迟
  - 最适合无需真实网络的测试

- **自定义适配器**：通过实现 `NetworkAdapter` 接口创建自己的适配器
  - 集成任何 HTTP 客户端库
  - 完全控制请求/响应处理
  - 最适合特殊需求

### 2. 统一的拦截器系统

新的 `AdapterInterceptor` 接口适用于所有适配器：

```dart
abstract class AdapterInterceptor {
  void onRequest(AdapterRequest request, RequestInterceptorHandler handler);
  void onResponse(AdapterResponse response, ResponseInterceptorHandler handler);
  void onError(AdapterException error, ErrorInterceptorHandler handler);
}
```

**优势：**
- 编写一次拦截器，可用于任何适配器
- 不依赖 Dio 特定类型
- 拦截器在底层 HTTP 客户端之前执行（保留所有请求信息）
- 支持请求修改、提前响应和错误处理


### 3. 改进的 CancelToken

新的独立 `CancelToken` 类，不依赖 Dio：

```dart
final cancelToken = CancelToken();

// 带可选原因的取消
cancelToken.cancel('用户取消');

// 检查是否已取消
if (cancelToken.isCancelled) {
  print('原因：${cancelToken.cancelReason}');
}

// 注册回调
cancelToken.whenCancel((reason) {
  print('请求已取消：$reason');
});
```

**注意：** DioAdapter 提供真正的取消（中止 HTTP 连接），而 HttpAdapter 提供伪取消（标记为已取消但请求继续）。

### 4. 枚举类型优化

所有枚举值现在使用大写，更符合 Dart 3.0+ 规范：

```dart
// ✅ 0.6.0（大写）
HttpMethod.GET, HttpMethod.POST
ResponseType.JSON, ResponseType.STREAM

// ❌ 0.5.x（小写 - 仍然有效但已弃用）
HttpMethod.get, HttpMethod.post
ResponseType.json, ResponseType.stream
```

### 核心优势

✅ **灵活性**：选择最适合您需求的 HTTP 客户端
✅ **可测试性**：使用 MockAdapter 轻松进行单元测试，无需网络调用
✅ **可扩展性**：为特殊需求创建自定义适配器
✅ **向后兼容**：现有代码无需更改即可工作（100% 兼容！）
✅ **更好的架构**：框架与 HTTP 实现之间的清晰分离
✅ **改进的拦截器**：统一系统，保留所有请求信息

---

## 🚨 破坏性变更

### 无！🎊

**RxNet Plus 0.6.0 与 0.5.x 100% 向后兼容**

所有现有代码将继续工作，无需任何修改。适配器架构在内部实现，默认使用 DioAdapter。

---

## 🔄 迁移步骤

### 步骤 1：更新依赖

更新您的 `pubspec.yaml`：

```yaml
dependencies:
  rxnet_plus: ^0.6.0  # 从 ^0.5.0 更新
```

运行：
```bash
flutter pub get
```

### 步骤 2：无需代码更改！✨

您的现有代码将按原样工作：

```dart
// 此代码在 0.5.x 和 0.6.0 中都有效
await RxNet.init(
  baseUrl: "https://api.example.com",
  baseCacheMode: CacheMode.REQUEST_FAILED_READ_CACHE,
  interceptors: [RxNetLogAdapterInterceptor()],
);

final response = await RxNet.get()
    .setPath('/users/123')
    .request();
```

### 步骤 3：（可选）探索新功能

如果您想使用新的适配器功能，请参阅下面的[新功能详解](#新功能详解)部分。

---

## 🆕 新功能详解

### 1. 显式适配器选择

您现在可以显式选择要使用的适配器：

#### 使用 DioAdapter（默认）

```dart
import 'package:rxnet_plus/adapters/dio_adapter.dart';

final adapter = DioAdapter();
await RxNet.init(
  baseUrl: 'https://api.example.com',
  adapter: adapter,  // 显式指定适配器
);
```

#### 使用 HttpAdapter（轻量级）

```dart
import 'package:rxnet_plus/adapters/http_adapter.dart';

final adapter = HttpAdapter();
await RxNet.init(
  baseUrl: 'https://api.example.com',
  adapter: adapter,
);
```

#### 使用 MockAdapter（测试）

```dart
import 'package:rxnet_plus/adapters/mock_adapter.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_response.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_request.dart';

void main() {
  test('应该获取用户数据', () async {
    final mockAdapter = MockAdapter();
    
    // 配置模拟响应
    mockAdapter.setMockResponse(
      '/users/123',
      AdapterResponse(
        statusCode: 200,
        data: {'id': 123, 'name': '张三'},
        headers: {},
        request: AdapterRequest(
          baseUrl: 'https://api.example.com',
          path: '/users/123',
          method: HttpMethod.GET,
        ),
      ),
    );
    
    await RxNet.init(
      baseUrl: 'https://api.example.com',
      adapter: mockAdapter,
    );
    
    final result = await RxNet.get()
        .setPath('/users/123')
        .request();
    
    expect(result.isSuccess, true);
    expect(result.value['name'], '张三');
  });
}
```

### 2. 使用不同适配器的多个网络实例

```dart
// 实例 1：使用 DioAdapter 的主 API
final mainApi = RxNet.create();
await mainApi.initNet(
  baseUrl: "https://api.example.com",
  adapter: DioAdapter(),
);

// 实例 2：使用 HttpAdapter 的轻量级请求
final lightApi = RxNet.create();
await lightApi.initNet(
  baseUrl: "https://cdn.example.com",
  adapter: HttpAdapter(),
);

// 使用不同实例
final userData = await mainApi.getRequest()
    .setPath('/users/123')
    .request();

final imageData = await lightApi.getRequest()
    .setPath('/images/avatar.jpg')
    .request();
```

### 3. 自定义适配器

为特殊需求创建自己的适配器：

```dart
import 'package:rxnet_plus/src/adapter/network_adapter.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_request.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_response.dart';

class MyCustomAdapter implements NetworkAdapter {
  @override
  String get name => 'MyCustomAdapter';
  
  @override
  String get version => '1.0.0';
  
  @override
  Future<AdapterResponse> request(AdapterRequest request) async {
    // 实现您的自定义 HTTP 逻辑
    // ...
  }
  
  // 实现其他必需方法...
}

// 使用您的自定义适配器
await RxNet.init(
  baseUrl: 'https://api.example.com',
  adapter: MyCustomAdapter(),
);
```

---

## 📊 适配器选择指南

### 何时使用 DioAdapter（默认）

✅ **使用 DioAdapter 当：**
- 您需要全功能的 HTTP 客户端能力
- 您想要最佳性能和功能
- 您正在构建生产应用
- 您需要高级功能，如拦截器、取消等

**要求：**
```yaml
dependencies:
  dio: ^5.8.0+1
```

### 何时使用 HttpAdapter

✅ **使用 HttpAdapter 当：**
- 您想最小化依赖
- 您只需要基本的 HTTP 功能
- 包大小是一个问题
- 您正在构建简单应用

**要求：**
```yaml
dependencies:
  http: ^1.2.0
```

### 何时使用 MockAdapter

✅ **使用 MockAdapter 当：**
- 您正在编写单元测试
- 您正在编写集成测试
- 您想在没有网络调用的情况下测试
- 您需要模拟特定场景

**要求：** 无（无外部依赖）

### 对比表

| 功能 | DioAdapter | HttpAdapter | MockAdapter |
|------|------------|-------------|-------------|
| 包大小 | 较大 | 较小 | 最小 |
| 功能 | 全功能 | 基础 | 仅测试 |
| 性能 | 优秀 | 良好 | N/A |
| 拦截器 | ✅ 是 | ✅ 是 | ✅ 是 |
| 文件上传/下载 | ✅ 是 | ✅ 是 | ✅ 模拟 |
| 进度回调 | ✅ 是 | ✅ 是 | ✅ 模拟 |
| 请求取消 | ✅ 真正取消 | ⚠️ 伪取消 | ✅ 模拟 |
| 高级功能 | ✅ 广泛 | ⚠️ 有限 | ❌ 无 |
| 测试支持 | ✅ 是 | ✅ 是 | ✅ 优秀 |

---

## 🔧 拦截器变更

### 新的 AdapterInterceptor 系统

0.6.0 引入了统一的 `AdapterInterceptor` 接口，适用于所有适配器：

#### 旧方式（0.5.x - Dio 特定）

```dart
import 'package:dio/dio.dart';

class MyInterceptor extends Interceptor {
  @override
  void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
    // Dio 特定代码
    options.headers['Authorization'] = 'Bearer token';
    handler.next(options);
  }
}

await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [MyInterceptor()],  // Dio 拦截器
);
```

#### 新方式（0.6.0 - 适配器无关）

```dart
import 'package:rxnet_plus/src/adapter/interceptor/adapter_interceptor.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_request.dart';
import 'package:rxnet_plus/src/adapter/models/adapter_response.dart';

class MyInterceptor implements AdapterInterceptor {
  @override
  void onRequest(AdapterRequest request, RequestInterceptorHandler handler) {
    // 适用于任何适配器！
    final modifiedRequest = request.copyWith(
      headers: {...request.headers, 'Authorization': 'Bearer token'},
    );
    handler.next(modifiedRequest);
  }
  
  @override
  void onResponse(AdapterResponse response, ResponseInterceptorHandler handler) {
    handler.next(response);
  }
  
  @override
  void onError(AdapterException error, ErrorInterceptorHandler handler) {
    handler.next(error);
  }
}

await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [MyInterceptor()],  // AdapterInterceptor
);
```

### 内置拦截器更新

**旧：** `RxNetLogInterceptor`（Dio 特定）
**新：** `RxNetLogAdapterInterceptor`（适用于所有适配器）

```dart
// ✅ 0.6.0 - 推荐
import 'package:rxnet_plus/net/interceptor/rxnet_log_adapter_interceptor.dart';

await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [RxNetLogAdapterInterceptor()],
);

// ⚠️ 0.5.x - 仍然适用于 DioAdapter 但已弃用
import 'package:dio/dio.dart';

await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [LogInterceptor()],  // Dio 的 LogInterceptor
);
```

### 拦截器执行流程（重要！）

在 0.6.0 中，拦截器在底层 HTTP 客户端**之前**执行：

```
用户请求
    ↓
BuildRequest 创建 AdapterRequest（包含所有参数）
    ↓
DioAdapter.request() 接收 AdapterRequest
    ↓
✅ 执行 AdapterInterceptors（可访问 bodyParams、pathParams 等）
    ↓
转换为 Dio RequestOptions
    ↓
调用 Dio.request()
    ↓
转换 Dio Response 为 AdapterResponse
    ↓
✅ 执行响应拦截器
    ↓
返回给用户
```

**优势：**
- 拦截器可以访问完整的请求信息（bodyParams、pathParams 等）
- 转换过程中无信息丢失
- 拦截器可以在发送前修改请求
- 在所有适配器中保持一致

### 拦截器处理器方法

```dart
class MyInterceptor implements AdapterInterceptor {
  @override
  void onRequest(AdapterRequest request, RequestInterceptorHandler handler) {
    // 选项 1：继续原始请求
    handler.next(request);
    
    // 选项 2：修改请求并继续
    final modified = request.copyWith(headers: {...});
    handler.next(modified);
    
    // 选项 3：直接返回响应（跳过实际请求）
    handler.resolve(AdapterResponse(...));
    
    // 选项 4：拒绝并返回错误
    handler.reject(AdapterException(...));
  }
  
  @override
  void onResponse(AdapterResponse response, ResponseInterceptorHandler handler) {
    // 选项 1：继续原始响应
    handler.next(response);
    
    // 选项 2：修改响应
    final modified = response.copyWith(data: {...});
    handler.next(modified);
    
    // 选项 3：拒绝并返回错误
    handler.reject(AdapterException(...));
  }
  
  @override
  void onError(AdapterException error, ErrorInterceptorHandler handler) {
    // 选项 1：继续错误
    handler.next(error);
    
    // 选项 2：修改错误
    final modified = error.copyWith(message: '自定义消息');
    handler.next(modified);
    
    // 选项 3：解决为响应（将错误转换为成功）
    handler.resolve(AdapterResponse(...));
  }
}
```

---

## 🎯 CancelToken 改进

### 新的独立 CancelToken

0.6.0 引入了独立于 Dio 的新 `CancelToken` 类：

#### 旧方式（0.5.x）

```dart
import 'package:dio/dio.dart';

final cancelToken = CancelToken();  // Dio 的 CancelToken

RxNet.get()
  .setPath('/users')
  .setCancelToken(cancelToken)
  .request();

cancelToken.cancel();  // 取消请求
```

#### 新方式（0.6.0）

```dart
import 'package:rxnet_plus/src/adapter/cancel_token.dart';

final cancelToken = CancelToken();  // RxNet 的 CancelToken

RxNet.get()
  .setPath('/users')
  .setCancelToken(cancelToken)
  .request();

// 带可选原因的取消
cancelToken.cancel('用户取消');

// 检查是否已取消
if (cancelToken.isCancelled) {
  print('已取消：${cancelToken.cancelReason}');
}

// 注册回调
cancelToken.whenCancel((reason) {
  print('请求已取消：$reason');
});
```

### 不同适配器的取消行为

**重要：** 不同适配器的取消行为不同：

| 适配器 | 取消类型 | HTTP 连接 | 带宽 |
|--------|---------|----------|------|
| **DioAdapter** | ✅ 真正取消 | 立即中止 | 节省 |
| **HttpAdapter** | ⚠️ 伪取消 | 继续运行 | 浪费 |
| **MockAdapter** | ✅ 真正取消 | N/A（无网络） | N/A |

**DioAdapter（推荐用于取消）：**
```dart
final adapter = DioAdapter();
await RxNet.init(baseUrl: "...", adapter: adapter);

final cancelToken = CancelToken();
RxNet.get()
  .setPath('/large-file')
  .setCancelToken(cancelToken)
  .download(savePath: '/path/to/file');

// ✅ 真正中止 HTTP 连接
cancelToken.cancel();
```

**HttpAdapter（有限取消）：**
```dart
final adapter = HttpAdapter();
await RxNet.init(baseUrl: "...", adapter: adapter);

final cancelToken = CancelToken();
RxNet.get()
  .setPath('/large-file')
  .setCancelToken(cancelToken)
  .download(savePath: '/path/to/file');

// ⚠️ 标记为已取消但 HTTP 请求继续
cancelToken.cancel();
```

**建议：** 对于需要真正取消的场景（大文件、长时间运行的请求），使用 DioAdapter。

---

## 🔄 常见迁移场景

### 场景 1：无需更改

**之前（0.5.x）：**
```dart
await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [RxNetLogInterceptor()],
);

final response = await RxNet.get()
    .setPath('/users')
    .request();
```

**之后（0.6.0）：**
```dart
// 相同代码有效！无需更改
await RxNet.init(
  baseUrl: "https://api.example.com",
  interceptors: [RxNetLogInterceptor()],
);

final response = await RxNet.get()
    .setPath('/users')
    .request();
```

### 场景 2：切换到 HttpAdapter

**之前（0.5.x）：**
```dart
await RxNet.init(
  baseUrl: "https://api.example.com",
);
```

**之后（0.6.0）：**
```dart
import 'package:rxnet_plus/adapters/http_adapter.dart';

await RxNet.init(
  baseUrl: "https://api.example.com",
  adapter: HttpAdapter(),  // 添加此行
);
```

### 场景 3：添加单元测试

**之前（0.5.x）：**
```dart
// 难以在没有真实网络调用的情况下测试
test('应该获取用户数据', () async {
  // 必须进行真实网络请求或模拟 Dio
});
```

**之后（0.6.0）：**
```dart
import 'package:rxnet_plus/adapters/mock_adapter.dart';

test('应该获取用户数据', () async {
  final mockAdapter = MockAdapter();
  mockAdapter.setMockResponse(
    '/users/123',
    AdapterResponse(
      statusCode: 200,
      data: {'id': 123, 'name': '张三'},
      headers: {},
      request: AdapterRequest(
        baseUrl: 'https://api.example.com',
        path: '/users/123',
        method: HttpMethod.GET,
      ),
    ),
  );
  
  await RxNet.init(
    baseUrl: 'https://api.example.com',
    adapter: mockAdapter,
  );
  
  final result = await RxNet.get()
      .setPath('/users/123')
      .request();
  
  expect(result.isSuccess, true);
  expect(result.value['name'], '张三');
});
```

### 场景 4：创建自定义日志拦截器

**之前（0.5.x）：**
```dart
import 'package:dio/dio.dart';

class CustomLogInterceptor extends Interceptor {
  @override
  void onRequest(RequestOptions options, RequestInterceptorHandler handler) {
    print('请求：${options.method} ${options.uri}');
    print('参数：${options.data}');  // 可能缺少 bodyParams
    handler.next(options);
  }
}
```

**之后（0.6.0）：**
```dart
import 'package:rxnet_plus/src/adapter/interceptor/adapter_interceptor.dart';

class CustomLogInterceptor implements AdapterInterceptor {
  @override
  void onRequest(AdapterRequest request, RequestInterceptorHandler handler) {
    print('请求：${request.method} ${request.buildFullUrl()}');
    print('查询参数：${request.queryParams}');
    print('Body 参数：${request.bodyParams}');  // ✅ 现在可用！
    print('路径参数：${request.pathParams}');   // ✅ 现在可用！
    handler.next(request);
  }
  
  @override
  void onResponse(AdapterResponse response, ResponseInterceptorHandler handler) {
    print('响应：${response.statusCode}');
    handler.next(response);
  }
  
  @override
  void onError(AdapterException error, ErrorInterceptorHandler handler) {
    print('错误：${error.message}');
    handler.next(error);
  }
}
```

---

## ❓ 常见问题

### 问：我需要更改现有代码吗？

**答：** 不需要！RxNet Plus 0.6.0 100% 向后兼容。您的现有代码将无需任何更改即可工作。

### 问：默认使用哪个适配器？

**答：** 默认使用 DioAdapter，保持与 0.5.x 相同的行为。

### 问：我可以在同一个应用中使用多个适配器吗？

**答：** 可以！您可以创建多个具有不同适配器的 RxNet 实例：

```dart
final mainApi = RxNet.create();
await mainApi.initNet(baseUrl: "...", adapter: DioAdapter());

final testApi = RxNet.create();
await testApi.initNet(baseUrl: "...", adapter: MockAdapter());
```

### 问：切换适配器会影响我的缓存吗？

**答：** 不会，缓存系统与适配器无关。您的缓存数据将适用于任何适配器。

### 问：我可以在运行时切换适配器吗？

**答：** 您需要使用新适配器重新初始化 RxNet。建议在应用启动时选择适配器。

### 问：拦截器适用于所有适配器吗？

**答：** 是的！拦截器系统与适配器无关，适用于所有适配器。

### 问：如果我想使用不同的 HTTP 客户端怎么办？

**答：** 您可以通过实现 `NetworkAdapter` 接口创建自定义适配器。详见[自定义适配器指南](docs/custom_adapter_guide.md)。

### 问：不同适配器之间有性能差异吗？

**答：** DioAdapter 通常提供最佳性能和功能。HttpAdapter 稍微轻量但功能较少。对于大多数应用，差异很小。

### 问：我仍然可以访问底层 Dio 实例吗？

**答：** 可以，如果您使用 DioAdapter：

```dart
final adapter = DioAdapter();
await RxNet.init(baseUrl: "...", adapter: adapter);

// 访问 Dio 实例
final dio = adapter.dio;
```

### 问：证书验证怎么办？

**答：** 证书验证的工作方式相同：

```dart
// 1. DioAdapter 示例：
final adapter = DioAdapter();
adapter.dio.httpClientAdapter = IOHttpClientAdapter(
  createHttpClient: () {
    final client = HttpClient();
    client.badCertificateCallback = (cert, host, port) {
      // 您的验证逻辑
      return true;
    };
    return client;
  },
);

// 2. HttpAdapter 示例：
IOClient createPinnedClient() {
  final HttpClient httpClient = HttpClient();
  httpClient.badCertificateCallback =
      (X509Certificate cert, String host, int port) {
    // 您的验证逻辑
    return true;
  };
  return IOClient(httpClient);
}

final adapter = HttpAdapter(client: createPinnedClient());
await RxNet.init(baseUrl: "...", adapter: adapter);
```

### 问：为什么我的拦截器现在可以看到 bodyParams 了？

**答：** 在 0.6.0 中，拦截器在底层 HTTP 客户端之前执行，因此它们可以访问原始的 `AdapterRequest`，其中包含所有参数（bodyParams、pathParams、queryParams 等）。在 0.5.x 中，拦截器通过 Dio 的系统执行，在转换过程中丢失了一些信息。

### 问：HttpAdapter 的伪取消是什么意思？

**答：** HttpAdapter 使用 Dart 的标准 `http` 包，该包不支持取消正在进行的请求。当您调用 `cancel()` 时，Future 会返回错误，但底层 HTTP 请求会继续直到完成。对于真正的取消（中止 HTTP 连接），请使用 DioAdapter。

---

## 📚 其他资源

- [适配器使用指南](lib/adapters/README.md)
- [DioAdapter 文档](lib/adapters/dio_adapter.dart)
- [HttpAdapter 文档](lib/adapters/http_adapter.dart)
- [MockAdapter 文档](lib/adapters/mock_adapter.dart)
- [自定义适配器教程](docs/custom_adapter_guide.md)
- [API 文档](https://pub.dev/documentation/rxnet_plus/latest/)

---

## 🆘 需要帮助？

如果您在迁移过程中遇到任何问题：

1. 查看上面的[常见问题](#常见问题)部分
2. 查看[适配器使用指南](lib/adapters/README.md)
3. 在 [GitHub](https://github.com/ZhengZaiHong/rxnet/issues) 上提出问题
4. 检查现有问题以查找类似问题

---

## 🎯 总结

**关键要点：**

✅ **无破坏性变更** - 您的现有代码按原样工作
✅ **新的灵活性** - 选择适合您需求的适配器
✅ **更好的测试** - 使用 MockAdapter 轻松进行单元测试
✅ **可扩展** - 在需要时创建自定义适配器
✅ **相同的强大功能** - 所有缓存、拦截器和功能仍然有效
✅ **改进的拦截器** - 可以访问完整的请求信息（bodyParams 等）
✅ **真正的取消** - DioAdapter 提供真正的 HTTP 连接中止

**推荐操作：**

1. 更新到 0.6.0（无需代码更改）
2. 探索 MockAdapter 以获得更好的单元测试
3. 如果包大小是问题，考虑 HttpAdapter
4. 阅读适配器文档以了解新功能
5. 对于需要真正取消的场景，使用 DioAdapter

欢迎使用 RxNet Plus 0.6.0！🚀
